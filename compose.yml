name: hermes-agent-manual

services:
  web:
    build:
      context: .
    image: hermes-agent-manual:local
    container_name: hermes-agent-manual
    restart: unless-stopped
    expose:
      - "80"
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1/health"]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 5s
    networks:
      - web

networks:
  web:
    external: true
    name: web

